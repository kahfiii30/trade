# Project Rules & Custom Guidelines

- **Auto Git Push**: Setiap kali selesai melakukan perubahan kode atau menyelesaikan permintaan user, selalu lakukan staging, commit, dan push langsung ke branch `main` di GitHub (`git add .`, `git commit -m "..."`, `git push origin main`).
